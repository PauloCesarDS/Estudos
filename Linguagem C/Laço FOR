#include<stdio.h>
#include<stdlib.h>

main()
{	//atividade:

int i, num, maior;
	
	printf("***Verifique o maior valor entre os (5) digitados***\n");
	
	// laco para controle de loop
	for(i =1; i <=5; i++)
{
	printf("\nDigite o numero:");
	scanf("%d", &num);	
	system("cls");	
	
	//comparacao dentro do for
	if(num >= maior)
	{
	maior = num;
	}
}
	printf("O maior valor digitado foi: %d",maior);
}
